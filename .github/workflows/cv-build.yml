name: Build CV PDF

on:
  push:
    paths:
      - 'Mohammed_Benouar_cv.tex'
      - '.github/workflows/cv-build.yml'
  workflow_dispatch:

permissions:
  contents: write   # <-- important

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0             # get full history
          persist-credentials: true  # uses GITHUB_TOKEN

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Pull latest changes
        run: git pull --rebase origin main

      - name: Compile LaTeX to PDF
        uses: xu-cheng/latex-action@v3
        with:
          root_file: Mohammed_Benouar_cv.tex
          latexmk_use_xelatex: true

      - name: Commit Mohammed_Benouar_cv.pdf
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "auto: update Mohammed_Benouar_cv.pdf"
          file_pattern: Mohammed_Benouar_cv.pdf
